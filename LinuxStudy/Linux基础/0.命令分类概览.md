### 1. **文件与目录操作**
处理文件和目录的基本操作
ls, cd, pwd, cp, mv, rm, mkdir, rmdir, touch
find, locate, whereis, which
cat, less, more, head, tail, nano, vim

### 2. **文本处理**
分析和处理文本内容
grep, awk, sed, cut, sort, uniq, wc, diff, patch
echo, printf, tee, tr, rev, fold

### 3. **系统信息与监控**
查看系统状态和性能
top, htop, ps, pstree, kill, killall, pkill
df, du, free, uptime, who, w, whoami
uname, hostname, date, cal, bc, factor

### 4. **权限与用户管理**
管理用户、组和权限
sudo, su, chmod, chown, chgrp, passwd
useradd, userdel, usermod, groupadd, id

### 5. **网络操作**

网络连接和通信
ping, ifconfig, ip, netstat, ss, traceroute, tracepath
curl, wget, ssh, scp, sftp, rsync, telnet
nc (netcat), nmap, iptables, firewall-cmd

### 6. **压缩与归档**

打包和压缩文件
tar, gzip, gunzip, bzip2, bunzip2, zip, unzip
xz, unxz, 7z, rar, unrar

### 7. **包管理**

软件包管理（发行版相关）
#### Debian/Ubuntu
apt, apt-get, apt-cache, dpkg
#### Red Hat/CentOS/Fedora
yum, dnf, rpm
#### Arch/Manjaro
pacman, yay, pamac
#### openSUSE
zypper

### 8. **磁盘管理**
磁盘分区和文件系统
fdisk, parted, mkfs, fsck, mount, umount
lsblk, blkid, df, du, dd

### 9. **进程与作业控制**
管理运行中的程序
&, bg, fg, jobs, nohup, disown
nice, renice, timeout, watch, screen, tmux

### 10. **环境与shell**
Shell环境相关
export, set, unset, alias, unalias, source, .
history, type, help, man, info, whatis, apropos
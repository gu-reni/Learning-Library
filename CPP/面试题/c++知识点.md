# C++ 面试题汇总

> 来源：牛客网面试经验  
> 更新时间：2026-03-18  
> 整理：C++ 面试真题精选  
> 收录公司：百度、微派、360、佳期投资、影石 Inst360、蔚来汽车、奇梦者科技等

---

## 目录

1. [智能指针](#1-智能指针)
2. [内存管理](#2-内存管理)
3. [进程与线程](#3-进程与线程)
4. [TCP/IP 网络](#4-tcpip-网络)
5. [STL 容器](#5-stl 容器)
6. [内存与变量存储](#6-内存与变量存储)
7. [模板与编译](#7-模板与编译)
8. [面向对象与多态](#8-面向对象与多态)
9. [C++11/17/20 新特性](#9-c111720-新特性)
10. [网络编程与 IO](#10-网络编程与-io)
11. [分布式系统](#11-分布式系统)
12. [AI/大模型相关](#12-ai 大模型相关)

---

## 1. 智能指针

### 1.1 shared_ptr 是线程安全的吗？引用计数和对象本身分别是什么情况？

**答案：**

- **引用计数是线程安全的：**
  - 控制块中的强引用计数和弱引用计数的增减是原子操作
  - 多个线程同时拷贝或销毁 shared_ptr 不会导致计数错误

- **对象本身不是线程安全的：**
  - 多个线程同时读同一个 shared_ptr 对象是安全的
  - 多个线程同时对同一个 shared_ptr 对象进行读写（比如一个线程在 reset，另一个在解引用）是未定义行为，需要加锁保护
  - shared_ptr 保护的是引用计数，不保护它指向的对象，对象的线程安全需要自己处理

- **常见误区：** 两个线程各自持有指向同一对象的不同 shared_ptr 副本，并发修改对象内容，这不是 shared_ptr 的责任范围，需要对象自己加锁。

---

### 1.2 为什么推荐用 make_shared 而不是直接 new 来构造 shared_ptr？

**答案：**

- **内存分配优化：** 直接 new 会进行两次内存分配（一次分配对象本身，一次分配控制块）；make_shared 只做一次内存分配，将对象和控制块放在同一块内存中，减少分配开销和内存碎片
- **异常安全：** `shared_ptr(new T(), deleter)` 这种写法，如果构造函数和 shared_ptr 构造之间抛异常，可能导致内存泄漏；make_shared 不存在这个问题
- **缓存友好：** 对象和控制块相邻，访问时更可能在同一缓存行

**缺点：**
- make_shared 的内存只有在强引用和弱引用都归零后才释放，若存在大量 weak_ptr，对象销毁后内存仍被控制块占用，可能延迟内存释放

---

### 1.3 C++ 智能指针的种类和区别，shared_ptr 的引用计数是如何实现的？

**答案：**

- **unique_ptr：** 独占所有权，不可拷贝只能移动，开销与裸指针几乎相同，是首选的智能指针
- **shared_ptr：** 共享所有权，内部维护引用计数，计数归零时自动释放资源
- **weak_ptr：** 弱引用，不增加引用计数，用于打破 shared_ptr 的循环引用，使用前需 lock() 升级为 shared_ptr

**shared_ptr 引用计数实现：**
- 内部有一个控制块（control block），存放强引用计数、弱引用计数和删除器
- 拷贝 shared_ptr 时强引用计数加一，析构时减一，减到 0 时销毁对象
- 弱引用计数归零时才释放控制块本身
- 引用计数的加减是原子操作，线程安全，但 shared_ptr 对象本身的读写不是线程安全的

---

### 1.4 weak_ptr 除了解决循环引用，在 lambda 中如何安全访问可能已销毁的对象？

**答案：**

- **核心问题：** lambda 捕获 shared_ptr 会延长对象生命周期；捕获裸指针或引用则对象销毁后访问是未定义行为
- **正确做法：** 捕获 weak_ptr，在 lambda 内部 lock() 升级为 shared_ptr 再使用：
  - lock() 成功说明对象还存在，返回有效的 shared_ptr
  - lock() 返回空说明对象已销毁，直接返回不做任何操作
  - 这样 lambda 不会延长对象生命周期，也不会出现悬空指针访问
- **典型场景：** 异步回调、定时器回调中访问发起请求的对象，对象可能在回调触发前已经析构

---

## 2. 内存管理

### 2.1 内存对齐是什么？为什么需要内存对齐？

**答案：**

- 内存对齐是指数据存放的起始地址必须是其自身大小的整数倍
- **原因一（硬件限制）：** 大多数 CPU 访问未对齐的内存需要多次总线操作，甚至直接触发异常
- **原因二（性能）：** 对齐的数据可以一次读取完成，未对齐可能需要两次读取再拼接
- **结构体对齐规则：** 每个成员按自身大小对齐，结构体整体大小是最大成员大小的整数倍
- 可以用 `#pragma pack` 或 `attribute((packed))` 强制取消对齐，但会牺牲性能
- 合理排列结构体成员顺序（大到小）可以减少填充字节，节省内存

---

### 2.2 堆和栈的区别是什么？

**答案：**

| 特性 | 栈 (Stack) | 堆 (Heap) |
|------|-----------|----------|
| 管理方式 | 编译器自动管理 | 程序员手动管理 (malloc/new) |
| 存储内容 | 局部变量、函数参数、返回地址 | 动态分配的对象 |
| 空间大小 | 有限（一般几 MB） | 大（受物理内存限制） |
| 分配速度 | 极快 | 较慢，开销更大 |
| 生命周期 | 函数调用结束自动释放 | 需要手动释放 |
| 碎片问题 | 无 | 可能产生碎片 |

---

### 2.3 全局变量、局部变量、静态变量分别存在哪里，如果不初始化默认是什么值？

**答案：**

| 变量类型 | 存储位置 | 未初始化默认值 |
|---------|---------|---------------|
| 全局变量（已初始化） | 数据段 | - |
| 全局变量（未初始化） | BSS 段 | 0 |
| 静态变量（已初始化） | 数据段 | - |
| 静态变量（未初始化） | BSS 段 | 0 |
| 局部变量 | 栈上 | 不确定（未定义行为） |
| 静态局部变量 | 静态存储区 | 0 |

**示例代码：**
```cpp
#include <stdio.h>

int g1;        // BSS，默认 0
int g2 = 10;   // 数据段
static int s1; // BSS，默认 0

void test() {
    int a;         // 栈上，未初始化，值不确定
    static int b;  // 静态存储区，默认 0
    printf("b = %d\n", b);
    b++;
}

int main() {
    printf("g1 = %d, g2 = %d, s1 = %d\n", g1, g2, s1);
    test();  // b = 0
    test();  // b = 1
    return 0;
}
```

---

### 2.4 除了堆和栈，进程地址空间里通常还有哪些区域？

**答案：**

一个进程的虚拟地址空间通常包括：
- **代码段：** 存放程序指令
- **数据段：** 已初始化的全局变量和静态变量
- **BSS 段：** 未初始化的全局变量和静态变量
- **堆：** 用于动态分配
- **栈：** 用于函数调用
- **共享库映射区：** 如 libc 通过 mmap 映射
- **匿名映射区：** 文件映射、共享内存、大块内存分配
- **只读常量区：** 常量字符串等
- **内核空间映射**
- **线程局部存储区 (TLS)**

---

### 2.5 什么是 RAII？在 C++ 中如何应用？

**答案：**

- **RAII（Resource Acquisition Is Initialization）** 是 C++ 核心资源管理思想
- **核心思想：** 资源的获取在对象构造时完成，资源的释放在对象析构时完成，利用栈对象的生命周期自动管理资源
- **应用场景：**
  - 智能指针管理动态内存
  - lock_guard 管理互斥锁
  - fstream 管理文件句柄
  - 自定义资源管理类

---

### 2.6 说说 new 和 malloc 的底层实现原理

**答案：**

- **malloc：** C 语言的内存分配函数，向运行时分配器申请一块指定大小的原始内存，返回 void*，只负责分配内存，不会调用构造函数
  - 底层分配器一般维护空闲链表或使用伙伴系统
  - 小块内存从堆上分配，大块内存（>128KB）通过 mmap 向系统申请
- **new：** C++ 运算符，先调用 operator new 分配内存，然后在内存上调用构造函数
  - operator new 底层通常调用 malloc
  - new 是类型安全的，返回具体类型的指针

---

## 3. 进程与线程

### 3.1 进程和线程有什么区别？

**答案：**

| 特性 | 进程 | 线程 |
|------|------|------|
| 资源分配 | 操作系统资源分配的基本单位 | 共享所属进程的资源 |
| 地址空间 | 独立的虚拟地址空间 | 共享进程地址空间 |
| 通信方式 | 需要 IPC 机制（管道、消息队列、共享内存等） | 直接读写共享内存 |
| 创建/切换开销 | 较大 | 较小 |
| 隔离性 | 一个进程崩溃不影响其他进程 | 一个线程崩溃可能导致整个进程崩溃 |
| 适用场景 | 需要强隔离、稳定性要求高 | 共享数据频繁的并发场景 |

---

### 3.2 进程、线程、协程有什么区别？

**答案：**

- **进程：** 资源分配的基本单位，有独立的虚拟地址空间、文件描述符表、堆栈等资源，进程之间隔离性强，但切换开销相对更大
- **线程：** CPU 调度的基本单位，同一进程内的线程共享地址空间和资源，切换开销较小
- **协程：** 用户态的轻量级线程，由程序员或运行时库调度，切换开销最小，但需要语言或库的支持

---

## 4. TCP/IP 网络

### 4.1 TCP 的流量控制和拥塞控制有什么区别？

**答案：**

- **流量控制：** 解决发送方和接收方之间的速度匹配问题，通过滑动窗口机制，接收方告诉发送方自己还能接收多少数据
- **拥塞控制：** 防止发送方发送过快导致网络拥塞，包括慢启动、拥塞避免、快重传、快恢复四个阶段

---

### 4.2 TCP 拥塞控制的机制是什么？

**答案：**

**四个阶段：**

1. **慢启动：** 连接建立后拥塞窗口（cwnd）从 1 开始，每收到一个 ACK 翻倍增长，指数增长直到达到慢启动阈值（ssthresh）
2. **拥塞避免：** cwnd 超过 ssthresh 后改为线性增长，每个 RTT 增加 1，避免增长过快
3. **快重传：** 收到 3 个重复 ACK 时立即重传丢失的包，不等超时，减少等待时间
4. **快恢复：** 快重传后将 ssthresh 设为当前 cwnd 的一半，cwnd 设为 ssthresh，继续拥塞避免而不是回到慢启动

**发生超时时：** ssthresh 减半，cwnd 重置为 1，重新进入慢启动，惩罚更重。

---

### 4.3 TCP 如何实现可靠传输？

**答案：**

TCP 通过以下机制保证可靠性：
- **序列号和确认号：** 每个字节都有序列号，接收方通过 ACK 确认收到的数据
- **超时重传：** 发送方为每个包设置定时器，超时未收到 ACK 则重传
- **校验和：** 检测数据是否损坏
- **流量控制：** 滑动窗口防止接收方缓冲区溢出
- **拥塞控制：** 防止网络过载

---

### 4.4 TCP 四次挥手的过程，为什么需要 TIME_WAIT 状态？

**答案：**

**四次挥手过程：**
1. 第一次：主动关闭方发送 FIN，进入 FIN_WAIT_1
2. 第二次：被动关闭方回复 ACK，主动方进入 FIN_WAIT_2，被动方进入 CLOSE_WAIT
3. 第三次：被动关闭方发送 FIN，进入 LAST_ACK
4. 第四次：主动关闭方回复 ACK，进入 TIME_WAIT，等待 2MSL 后进入 CLOSED

**TIME_WAIT 的原因：**
- 确保最后一个 ACK 能到达对端，若对端没收到会重传 FIN，TIME_WAIT 期间可以重新回复
- 让网络中残留的旧数据包自然消亡，防止被新连接误收，2MSL 足够让所有报文过期

---

### 4.5 TCP 丢包之后一般怎么处理？

**答案：**

TCP 是可靠传输协议，不会把"丢包"直接暴露给应用层，而是通过以下机制自己处理：
- **确认和重传：** 最基本的手段是超时重传
- **快速重传：** 收到 3 个重复 ACK 时立即重传
- **滑动窗口：** 管理已发送但未确认的数据

---

### 4.6 说说 epoll 的工作原理，和 select/poll 相比有什么优势？

**答案：**

- **select/poll 的问题：**
  - 每次调用都需要把全部 fd 从用户态拷贝到内核态
  - 返回后需要遍历所有 fd 找到就绪的，时间复杂度 O(n)
  - select 有 fd 数量上限（1024）

- **epoll 的优势：**
  - 通过 epoll_create 创建实例，epoll_ctl 注册感兴趣的事件，epoll_wait 只返回就绪的 fd
  - 内核用红黑树管理注册的 fd，用就绪链表存放活跃事件
  - 每次只拷贝就绪事件到用户态，时间复杂度接近 O(1)

- **epoll 两种模式：**
  - **LT（水平触发）：** 默认，只要缓冲区有数据就持续通知
  - **ET（边缘触发）：** 只在状态变化时通知一次，需要一次性读完数据，性能更高但编程复杂

---

## 5. STL 容器

### 5.1 STL 迭代器失效在哪些场景下会发生？以 vector 为例

**答案：**

**插入操作：**
- 在末尾 push_back 时，若触发扩容（重新分配内存），所有迭代器、指针、引用全部失效
- 在中间 insert 时，插入位置之后的所有迭代器失效，若触发扩容则全部失效

**删除操作：**
- erase 删除某个元素后，该位置及之后的所有迭代器失效，之前的迭代器仍然有效

**正确做法：**
- erase 返回值是指向下一个有效元素的迭代器，删除时应使用返回值更新迭代器
- 遍历时删除元素用 `it = vec.erase(it)` 而不是 `vec.erase(it); it++`
- 其他容器：map/set 删除元素只失效被删除的迭代器；list 插入删除不影响其他迭代器

---

## 6. 内存与变量存储

### 6.1 说说 C++ 内存模型中的内存序，relaxed、acquire、release、seq_cst 分别是什么含义？

**答案：**

- **relaxed：** 只保证操作本身的原子性，不提供任何同步和顺序保证，适合只需要计数、不需要同步的场景，性能最高
- **release：** 写操作，保证该操作之前的所有读写不会被重排到它之后，配合 acquire 使用
- **acquire：** 读操作，保证该操作之后的所有读写不会被重排到它之前，配合 release 使用
- **release + acquire 配对：** 线程 A 用 release 写，线程 B 用 acquire 读到该值后，能看到线程 A 在 release 之前的所有写操作，这是最常用的同步手段
- **seq_cst：** 最强的内存序，所有 seq_cst 操作在所有线程中有一个全局一致的顺序，性能最差，是 atomic 操作的默认内存序

**实际工程：** 实现无锁数据结构时用 acquire/release 精确控制同步点，避免 seq_cst 的全局排序开销。

---

### 6.2 说说 Linux 虚拟内存机制，mmap 的原理是什么？

**答案：**

**虚拟内存：**
- 每个进程有独立的虚拟地址空间，通过页表映射到物理内存
- 访问未映射的地址触发缺页异常，内核分配物理页并建立映射
- 写时复制（COW）：fork 后父子进程共享物理页，任意一方写时才复制，节省内存

**mmap 原理：**
- mmap 在进程虚拟地址空间中建立一段映射，可以映射文件或匿名内存
- 映射文件时，读写该内存区域等同于读写文件，内核负责页面换入换出，避免 read/write 的用户态内核态拷贝
- 匿名 mmap 用于大块内存分配，malloc 内部对大于 128KB 的请求就用 mmap
- munmap 释放映射，对于文件映射会将脏页写回磁盘
- 多个进程 mmap 同一文件可以实现共享内存，是高效 IPC 的基础

---

## 7. 模板与编译

### 7.1 说说 C++ 模板的实例化机制，什么是模板的二阶段编译？

**答案：**

**二阶段编译：**
- **第一阶段（模板定义时）：** 编译器检查与模板参数无关的语法错误，比如缺少分号、未声明的非依赖名称
- **第二阶段（模板实例化时）：** 编译器用具体类型替换模板参数，检查依赖于模板参数的代码是否合法

**实例化机制：**
- **隐式实例化：** 使用模板时编译器自动生成对应类型的代码，每个编译单元可能各自实例化一份，链接时去重
- **显式实例化：** 用 `template class MyClass` 强制在当前编译单元生成实例，其他编译单元用 `extern template` 声明避免重复实例化，减少编译时间
- 模板代码必须放在头文件中，因为实例化需要看到完整定义

**SFINAE（替换失败不是错误）：**
- 模板参数替换失败时不报错，只是从候选集中移除该重载
- 配合 enable_if 实现编译期条件选择

---

## 8. 其他高频考点

### 8.1 哈希函数主要解决什么问题？

**答案：**

- 哈希函数的核心作用是把一个范围很大的 key，映射成一个相对固定范围内的整数下标，方便快速存储和查找
- 它解决的是"如何尽量快地定位数据"问题
- 理想情况下，希望不同 key 经过哈希后分布尽量均匀，这样哈希表的插入、查找、删除平均复杂度可以做到 O(1)
- 哈希函数不能避免冲突，只能尽量减少冲突，所以哈希表还要有冲突处理机制

---

### 8.2 哈希冲突一般怎么处理，扩容时会发生什么？

**答案：**

**冲突处理方式：**
- **拉链法：** 每个桶挂一个链表或其他容器
- **开放寻址法：** 线性探测、二次探测等

**扩容：**
- 当负载因子变高，冲突增多时，哈希表通常会扩容
- 扩容需要把原来所有元素重新计算桶位置，这个过程叫 rehash
- 哈希表平均 O(1) 的前提是负载因子控制得当，哈希函数分布均匀

---

### 8.3 虚函数与多态

**常见考点：**
- 虚函数表（vtable）的实现原理
- 虚函数表的布局
- 构造函数和析构函数中调用虚函数的行为
- 纯虚函数与抽象类
- override 和 final 关键字

---

### 8.4 面向对象基础

**常见考点：**
- 封装、继承、多态的概念
- 公有继承、私有继承、保护继承的区别
- 虚继承解决菱形继承问题
- 深拷贝与浅拷贝
- 拷贝构造函数、拷贝赋值运算符、移动构造函数、移动赋值运算符（Rule of Five）

---

### 8.5 关键字辨析

**常见考点：**
- const 的作用（修饰变量、指针、成员函数、参数）
- static 的作用（修饰局部变量、全局变量、类成员）
- inline 的作用和内联展开的条件
- explicit 防止隐式转换
- volatile 的作用
- constexpr 与 const 的区别

---

## 10. 网络编程与 IO

### 10.1 TCP 三次握手的过程是什么？为什么不能是两次？

**答案：**

**过程：**
- 第一次：客户端发送 SYN，进入 SYN_SENT 状态
- 第二次：服务端收到后回复 SYN+ACK，进入 SYN_RCVD 状态
- 第三次：客户端回复 ACK，双方进入 ESTABLISHED 状态

**为什么不能是两次：**
- 两次握手只能确认客户端到服务端的通路正常，无法确认服务端到客户端方向
- 三次握手让双方都能确认自己的发送和接收能力均正常
- 同时可以防止历史失效的连接请求被服务端误认为新连接，造成资源浪费

---

### 10.2 TIME_WAIT 状态的作用是什么，如果有大量 TIME_WAIT 怎么处理？

**答案：**

**TIME_WAIT 的作用：**
- 确保最后一个 ACK 有机会被对方收到。如果主动关闭方发送的最后 ACK 丢失，被动关闭方还会重发 FIN，这时主动关闭方还可以重新回复 ACK
- 让旧连接中的延迟报文在网络中自然消失，避免它们影响后续使用相同四元组建立的新连接

**大量 TIME_WAIT 处理方案：**
- 优先考虑连接复用、长连接、连接池
- 通过系统参数适当调优：缩短 TIME_WAIT 保留时间、扩大本地端口范围（要谨慎，不能破坏协议语义）
- 真正稳定的方案是减少无意义短连接，而不是单纯依赖内核参数硬压

---

### 10.3 malloc 的底层实现机制是什么？

**答案：**

- **小块内存（通常 <= 128KB）：** 通过 brk/sbrk 从堆上分配，内部按大小维护多个 bin，分配时优先从合适的 bin 中复用空闲块
- **大块内存（> 128KB）：** 通过 mmap 直接映射匿名内存，释放时 munmap 归还操作系统
- 频繁分配释放不同大小的内存会产生内存碎片，是 malloc 的主要问题之一

---

### 10.4 有 1 万个下载任务，单进程如何合理分配线程数？

**答案：**

- 下载属于 I/O 密集型任务，线程大部分时间在等待网络响应，CPU 占用低
- 线程数不是越多越好，过多会导致内存占用高（每个线程默认栈约 1~8MB）、上下文切换频繁
- **经验公式：** 线程数 = CPU 核心数 × (1 + 等待时间 / 计算时间)，I/O 密集型可适当放大倍数
- **实际做法：** 用线程池（如 100~500 个线程）配合任务队列，1 万个任务排队分批消费
- **更优方案：** 异步 I/O（epoll + 协程），用少量线程处理大量并发，避免线程膨胀

---

### 10.5 进程间通信有哪些方式？各自适用场景是什么？

**答案：**

| IPC 方式 | 特点 | 适用场景 |
|---------|------|---------|
| 管道 (pipe) | 适合父子进程间单向通信，简单但只能传字节流 | 父子进程通信 |
| 消息队列 | 支持结构化消息，解耦发送和接收方 | 需要消息持久化 |
| 共享内存 | 最快，避免数据拷贝，但需要同步机制 | 大数据量、高频通信 |
| 信号量 | 用于同步，不传输数据 | 进程间同步 |
| 信号 (signal) | 异步通知机制 | 进程控制 |
| 套接字 (socket) | 支持跨机器通信 | 网络通信、本地 IPC |

---

### 10.6 Reactor 和 Proactor 模型的区别

**答案：**

- **Reactor 模型：** 强调"事件就绪通知"。内核告诉你某个 fd 可读或可写了，真正的读写操作还是由应用程序自己去完成
- **Proactor 模型：** 更像"异步操作完成通知"。应用先发起异步读写，内核或底层系统在操作真正完成之后再通知应用
- **Linux 实践：** 传统 Linux 网络编程多用 Reactor（epoll），Windows IOCP 是典型的 Proactor

---

### 10.7 用事件驱动模型实现一个定时器，思路是什么？

**答案：**

- 核心思路不是"开一个线程 sleep"，而是把时间事件纳入统一事件循环
- 维护一个按触发时间排序的数据结构：小根堆、时间轮、红黑树
- 事件循环每次处理 I/O 事件之前，先看当前最近一个定时器还有多久到期，把这个时间作为 epoll_wait 的超时时间
- 超时返回后检查哪些定时器到期，执行回调

**数据结构选择：**
- **小根堆：** 适合通用定时器实现，插入和取最近到期元素都比较直接
- **时间轮：** 更适合大量定时任务且精度要求可控的场景

---

### 10.8 在共享内存上直接分配 vector 会有什么问题？

**答案：**

- **核心问题：** vector 内部真正存数据的那块连续内存，默认还是通过当前进程的分配器去申请的（进程自己的堆），不一定在共享内存区域内
- **结果：** vector 对象本身放在共享内存里，但它内部的 data 指针指向的是某个进程私有地址空间，其他进程根本没法正确访问
- **更合理的做法：**
  - 使用共享内存专用分配器、偏移指针
  - 自己设计定长数组/环形缓冲区结构
  - 使用 Boost.Interprocess 这类专门解决共享内存容器问题的库

---

## 11. 分布式系统

### 11.1 了解微服务架构吗，简单说说 CAP 理论

**答案：**

- **微服务架构：** 把一个大系统拆成一组职责更清晰、可以独立部署和演进的小服务
  - 好处：模块边界更清晰、团队协作更灵活、局部迭代和扩缩容更方便
  - 代价：分布式系统复杂度提升（服务发现、链路追踪、配置管理、熔断限流、分布式事务等）

- **CAP 理论：** 在分布式系统里，一致性 (Consistency)、可用性 (Availability)、分区容错性 (Partition tolerance) 三者不能同时做到完美
  - 分区容错在真实分布式环境里几乎是必须接受的前提
  - 实际是在一致性和可用性之间做取舍
  - **CP 系统：** 优先保证数据一致（如 ZooKeeper）
  - **AP 系统：** 在网络分区时接受短暂不一致，后续再做收敛（如 Cassandra）

---

### 11.2 了解过 Raft 协议吗，简单说说它解决了什么问题

**答案：**

- **核心目标：** 在多个节点之间就"日志顺序"和"状态机执行结果"达成一致
- **解决的问题：** 在分布式环境下，如何在节点宕机、网络抖动等情况下仍然选出一个领导者，并让集群中的多数节点对操作顺序达成一致
- **三种角色：**
  - **Leader：** 接收客户端请求，把日志复制给其他节点，等多数节点确认后提交
  - **Follower：** 被动接收 Leader 的日志复制
  - **Candidate：** Follower 超时后发起选举时成为 Candidate
- **相比 Paxos：** Raft 的工程可理解性更强，很多分布式系统和 KV 存储都会采用它

---

## 12. AI/大模型相关

### 12.1 你怎么理解 AI、LLM、大模型工作流、MCP、Agent、Prompt？

**答案：**

| 概念 | 说明 |
|------|------|
| **AI** | 更大的集合，机器学习、深度学习、计算机视觉、推荐系统、语音识别都算 AI |
| **LLM** | 大语言模型，是 AI 里专门处理自然语言、代码、推理、问答这类任务的一类基础模型 |
| **Prompt** | 给模型的输入指令设计，决定模型如何理解任务、输出什么格式、遵循什么约束 |
| **工作流** | 把多个步骤串起来（意图识别、知识检索、工具调用、结果校验、格式化输出），强调流程编排和稳定交付 |
| **Agent** | 带有目标、自主决策、记忆、规划、工具使用能力，会根据环境反馈动态决定下一步做什么 |
| **MCP** | 让模型更规范地接入外部工具、数据源、上下文能力的协议化方式 |

**一句话概括：** LLM 是大脑，Prompt 是输入约束，工作流是流水线，Agent 是会做决策的执行体，MCP 是它接外部世界的标准接口

---

### 12.2 大模型的原理是什么？

**答案：**

- **核心：** "根据已有上下文预测下一个 token 的概率分布"
- **架构基础：** Transformer 架构，关键机制是自注意力（Self-Attention）
  - 自注意力让模型在处理当前 token 时，同时关注上下文里哪些位置更重要，从而学到长距离依赖关系
- **训练阶段：**
  - **预训练：** 在海量文本上学习语言规律、知识分布和模式
  - **后训练：** 指令微调、偏好对齐、RLHF 或 DPO 等方法让输出更符合人类期望
- **推理阶段：** 不断把前面已经生成的内容重新送回模型，接着预测下一个 token
- **工程优化：** KV Cache、量化、并行推理、投机采样等

---

### 12.3 如果让你设计一个企业知识库问答 Agent，你会怎么做？

**答案：**

- **采用 RAG 架构：** 先把企业文档做清洗、切分、向量化和索引构建，查询时先检索相关片段，再把检索结果和用户问题一起送进模型生成答案
- **工具层：** 数据库查询、工单系统、代码仓库搜索、权限校验、网页抓取、结构化 API 调用
- **系统层面考虑：**
  - 多轮对话记忆怎么存
  - 检索召回和重排怎么做
  - 答案引用来源怎么展示
  - 敏感信息怎么脱敏
  - 幻觉怎么降低
  - 用户问题失败时怎么回退到关键词检索
- **企业场景难点：** 不是"模型会不会答"，而是"答得稳不稳、准不准、能不能审计、权限是否隔离"

---

### 12.4 Prompt 提示词设计时你会注意什么？

**答案：**

- **目标明确：** 把任务说清楚
- **上下文充分：** 依赖背景知识时要补上下文，而不是让模型自己猜
- **约束清晰：** 结构化输出要把格式约束写清楚（JSON、Markdown、字段名固定等）
- **输出可控：** 告诉模型身份、目标、边界、禁止项和优先级
- **鲁棒性：** 能面对脏数据、模糊问题、恶意输入和超长上下文
- **工程思维：** Prompt 只是系统的一部分，该靠检索解决的就别硬塞上下文，该靠工具调用解决的就别让模型瞎编，该靠规则校验的就别完全放给模型自由发挥

---

### 12.5 Agent 和传统工作流的区别

**答案：**

| 特性 | 传统工作流 | Agent |
|------|-----------|-------|
| **流程** | 预先画好的流程图，节点和路径大多是固定的 | 根据目标、上下文和工具反馈动态决定下一步行动 |
| **优势** | 稳定、可控、易审计 | 自主决策、多步思考、工具协作 |
| **适用场景** | 规则明确、流程固定的业务（审批、分类、标准化抽取） | 任务路径不确定、需要多步思考和工具协作的场景 |
| **实践建议** | 外层用工作流兜住边界和状态，局部节点再让 Agent 做更灵活的决策 |

---

### 12.6 vector 交换资源，swap 和 move 的区别是什么？

**答案：**

- **move：** 把一个对象的资源转移给另一个对象，让源对象进入一种"有效但未指定"的状态
  - 场景：用一个已有 vector 初始化另一个 vector，新对象接管旧对象的 buffer
- **swap：** 两个对象互换内部资源，交换完成后双方都仍然是完整对象，只是内容互换了
  - 场景：快速交换内容、缩容技巧、异常安全写法
- **复杂度：** vector::swap 通常是常数复杂度（主要交换内部指针和状态）；移动构造也很轻量，但语义不同

---

### 12.7 RVO（返回值优化）了解吗？

**答案：**

- **RVO（Return Value Optimization）：** 编译器优化技术，避免函数返回对象时的拷贝
- **NRVO（Named RVO）：** 对命名返回值的优化
- **C++17 保证：** 某些情况下拷贝消除是强制要求的

---

## 面试技巧建议

1. **理解原理而非死记硬背：** 面试官更看重你对底层原理的理解
2. **结合项目经验：** 能结合实际项目说明技术选型和问题解决过程
3. **手写代码能力：** 常见算法和数据结构要能手写实现
4. **系统性思考：** 能从多个角度分析问题，给出权衡方案

---

> **备注：** 本汇总基于牛客网真实面试经验分享整理，题目难度覆盖初级到高级 C++ 开发岗位。建议结合自身技术栈有针对性地准备。

---

## 附录：面经来源

- 百度 C++ 软件开发 一面/二面
- 微派 c++ 软件开发 一面/二面
- 360 软件开发-C++ 一面
- 佳期投资 C++ 开发 一面
- 影石 Inst360 音视频开发 一面/二面
- 蔚来汽车 自动驾驶-C++ 一面
- 奇梦者科技 C++ 开发 一面
